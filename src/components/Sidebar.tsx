import type { FyQuarter } from '../types'

type Props = { from: string; to: string; quarter: FyQuarter | 'All'; metric: string; months: string[]; onChange: (key: string, value: string) => void; onClear: () => void; onAdd: () => void; onExport: () => void }
export function Sidebar({ from, to, quarter, metric, months, onChange, onClear, onAdd, onExport }: Props) {
  return <aside className="sidebar"><div className="side-brand"><span>DCCT</span><small>HTA / 2026</small></div><div className="side-rule" /><p className="side-label">FILTER VIEW</p><label>From month<select value={from} onChange={(e) => onChange('from', e.target.value)}><option value="">All months</option>{months.map((month) => <option key={month}>{month}</option>)}</select></label><label>To month<select value={to} onChange={(e) => onChange('to', e.target.value)}><option value="">All months</option>{months.map((month) => <option key={month}>{month}</option>)}</select></label><label>FY quarter<select value={quarter} onChange={(e) => onChange('quarter', e.target.value)}><option>All</option><option>Q1</option><option>Q2</option><option>Q3</option><option>Q4</option></select></label><label>Chart metric<select value={metric} onChange={(e) => onChange('metric', e.target.value)}><option value="reach">Client reach</option><option value="hivTests">HIV tests</option><option value="condomsMale">Male condoms</option><option value="condomsFemale">Female condoms</option></select></label><button className="clear-button" onClick={onClear}>Clear filters</button><div className="side-actions"><button className="primary-button" onClick={onAdd}>+ Add month</button><button className="outline-button" onClick={onExport}>Export CSV</button></div><p className="side-footer">Shared programme workspace<br />Cape Town, South Africa</p></aside>
}
