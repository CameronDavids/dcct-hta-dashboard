import { Bar, CartesianGrid, ComposedChart, LabelList, Legend, Line, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import type { HtaMonth } from '../types'
import { formatMonthLabel, performancePercent } from '../lib/utils'

type Props = { months: HtaMonth[]; metric: string }
export function PerformanceChart({ months, metric }: Props) { const data = months.map((item) => ({ ...item, label: formatMonthLabel(item.year, item.month), actual: item[metric as keyof HtaMonth] as number ?? 0, performance: performancePercent(item.reach, item.target) }))
  return <section className="chart-panel"><div className="panel-heading"><div><p className="eyebrow">MONTHLY TREND</p><h2>Actual against target</h2></div><span className="chart-note">Bars: selected metric / Line: reach %</span></div><div className="chart-wrap"><ResponsiveContainer width="100%" height="100%"><ComposedChart data={data} margin={{ top: 18, right: 12, left: 0, bottom: 5 }}><CartesianGrid stroke="#e4e9f0" vertical={false} /><XAxis dataKey="label" tick={{ fill: '#637083', fontSize: 11 }} axisLine={false} tickLine={false} /><YAxis yAxisId="left" tick={{ fill: '#637083', fontSize: 11 }} axisLine={false} tickLine={false} /><YAxis yAxisId="right" orientation="right" domain={[0, 200]} tickFormatter={(value) => `${value}%`} tick={{ fill: '#637083', fontSize: 11 }} axisLine={false} tickLine={false} /><Tooltip /><Legend /><Bar yAxisId="left" dataKey="actual" name="Actual" fill="#1f3864" radius={[3, 3, 0, 0]}><LabelList dataKey="actual" position="top" fill="#1f3864" fontSize={10} /></Bar><Bar yAxisId="left" dataKey="target" name="Target" fill="#b8c4d5" radius={[3, 3, 0, 0]} /><Line yAxisId="right" type="monotone" dataKey="performance" name="Performance %" stroke="#d56b43" strokeWidth={2.5} strokeDasharray="5 4" dot={{ r: 3, fill: '#d56b43' }} /></ComposedChart></ResponsiveContainer></div></section>
}
